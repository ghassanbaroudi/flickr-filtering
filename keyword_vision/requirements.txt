# Keyword + vision filter (install in your project venv).
# `metadata_keywords` loads clusters_compare (needs sklearn + numpy + pandas).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
python-dotenv>=1.0
requests>=2.31
# Optional: only for --backend openai
openai>=1.40
# Default vision backend (--backend openclip): install PyTorch for your platform first if needed
# https://pytorch.org/get-started/locally/
torch>=2.0
open-clip-torch>=2.24.0
Pillow>=9.0
tqdm>=4.65
